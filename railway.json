{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m venv /app/venv39 && /app/venv39/bin/pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "/app/venv39/bin/python bot.py"
  }
}